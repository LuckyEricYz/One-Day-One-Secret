# 用户交互流程

> 当前版本采用“角色先行、首页直达”的极简链路。

## 1. 总体链路

```text
首次使用：
打开应用
  -> 读取 tianji_v2_role_id / tianji_v2_history / tianji_v2_modal_seen
  -> 选择固定角色
  -> 自动弹出今日卦象
  -> 进入首页四模块
  -> 可打开历史快照

非首次使用：
打开应用
  -> 读取上次角色
  -> 生成今日快照
  -> 自动弹出今日卦象
  -> 进入首页四模块
  -> 可切换角色 / 打开历史快照
```

## 2. 启动逻辑

1. 若不存在 `tianji_v2_role_id`，进入角色选择页
2. 若已存在角色，直接生成当天快照
3. 若当天角色弹窗未读，默认弹出今日卦象
4. 历史快照始终可从头部入口查看

## 3. 角色选择

固定两个角色：

- 男角色：`乾行先生`
- 女角色：`和宁女士`

角色卡必须展示：

- 基础状态
- 流年 / 八字摘要
- 年度宜忌
- 情绪特质

要求：

- 不再要求用户手动输入任何信息
- 点击角色后立即进入当天内容
- 切换角色时重新计算当天快照

## 4. 今日卦象弹窗

### 4.1 触发规则

- 首次选择角色后立即弹出
- 已有角色时冷启动默认弹出
- 同一角色同一天关闭后，本会话和刷新后都不重复打断
- 跨日或切换角色后再次弹出

### 4.2 展示内容

必含：

- 卦名
- 六爻图
- 一句今日主轴
- 一段通俗养生解释
- 2-3 个焦点标签

## 5. 首页四模块

首页固定展示以下 4 块：

1. `当日一卦`
2. `带薪健身`
3. `每日穴位`
4. `节气食谱`

布局要求：

- 手机端单列
- 桌面端 2x2 卡片
- 不再有问卷、成卦、补录、结果卡的分步骤切屏

## 6. 每日自动更新

- 以 `Asia/Shanghai` 自然日 `00:00` 为准
- 跨日后重新生成 `DailySnapshot`
- 重新触发当天角色的卦象弹窗
- 同一天重复打开只复用同一份快照

## 7. 历史快照

### 7.1 规则

- 以 `roleId + dateKey` 作为唯一键
- 每个角色每天只保留一条记录
- 列表按日期倒序显示

### 7.2 展示

- 日期
- 角色标签
- 卦名
- 当天动作、穴位、食谱摘要

## 8. 本地存储

```text
tianji_v2_role_id
tianji_v2_history
tianji_v2_modal_seen
```
